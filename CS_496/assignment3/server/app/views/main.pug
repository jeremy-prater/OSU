doctype html
html(lang="en")
  head
    title="CS 496 OAuth 2.0 Demo"
    style
      include ../../bootstrap/bootstrap.min.css
      include ../../css/main.css
    script(src='../../bootstrap/jquery-3.2.1.slim.min.js')
    script(src='../../bootstrap/popper.min.js')
    script(src='../../bootstrap/bootstrap.min.js')
    script(src='../../client/state.js')
  body
    nav(class="navbar navbar-dark bg-dark viewNavbar")
      a(class="navbar-brand" href="#")
        | Magic OAuth 2.0 doughnut (from that particular bakery)
      div(class="form-inline")
        button(class="btn btn-outline-success" onclick="console.log(currentState); currentState.StartOAuth(currentState.superSecret);")
          | Sign in

    div(class="viewContainer container-fluid" id="body")
      img(src="../../assets/knucks.png" class="imgfitparent")

    div(id="myModal" class="modal fade" role="dialog")
      div(class="modal-dialog")
        div(class="modal-content")
          div(class="modal-header")
            h4(class="modal-title")
              | OAuth 2.0 Demo
            button(type="button" class="close" data-dismiss="modal")
              | &times;
          div(class="modal-body")
            p
              | This application will demonstrate how to authenticate with Google's OAuth 2.0 framework.
            p
              | Please close this dialog and click the sign in button on the top right of this page.
            p
              | Note: Some google profiles do not have a "displayName" value set. In this case, your name will be replaced with "Anonymous"
          div(class="modal-footer")
            button(type="button" class="btn btn-default" data-dismiss="modal")
              | Close
    script
      | $('#myModal').modal('show');